@model ICategoryRepository

@*<li>
    <a class="tab"
       asp-action="Index"
       asp-controller="Admin"
       asp-route-category="">
        <span style="font-weight:bold;">
            All products
        </span>
    </a>
</li>*@
@foreach (var p in Model.ParentCategories)
{
<li>
    <a class="tab"
       asp-action="List"
       asp-controller="Admin"
       asp-route-category="@p.Name"
       asp-route-page="1">
        <span style="">
            @p.Name
        </span>
    </a>
    @foreach (var c in p.ChildCategories)
    {
        <a class="tab" asp-action="List" asp-controller="Admin" asp-route-category="@c.Name">@c.Name</a>
        @*@foreach (var cChild in c.ChildCategories)
        {
            <a class="tab" asp-action="List" asp-controller="Admin" asp-route-category="@cChild.Name">@cChild.Name</a>
        }*@
    }
</li>

}
@*@foreach (Category category in Model)
    {
        <li class=" nav-justified">
            <a class= "tab"
               asp-action="List"
               asp-controller="Admin"
               asp-route-category="@category.Name"
               asp-route-page="1">
                <span style="">
                    @category.Name
                </span>
            </a>
        </li>
    }*@
